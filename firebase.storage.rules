rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /satsang/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null
        && (request.auth.token.email == 'ajaybhatnagar1712@gmail.com');
    }
  }
}
